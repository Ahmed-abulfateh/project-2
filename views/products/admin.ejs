<!DOCTYPE html>
<html>
<head>
  <title>Admin Dashboard - Products</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('../navbar.ejs') %>
  
  <div class="container">
    <h1>Admin Dashboard - Manage Products</h1>
    
    <form action="/products" method="POST" class="create-product-form">
      <h3>Add New Product</h3>
      <input type="text" name="name" placeholder="Product Name" required>
      <textarea name="description" placeholder="Description" required></textarea>
      <input type="number" name="price" placeholder="Price" step="0.01" required>
      <input type="number" name="stock" placeholder="Stock Quantity" required>
      <select name="category" required>
        <option value="">Select Category</option>
        <option value="Electronics">Electronics</option>
        <option value="Clothing">Clothing</option>
        <option value="Home & Garden">Home & Garden</option>
        <option value="Sports">Sports</option>
        <option value="Books">Books</option>
        <option value="Toys">Toys</option>
        <option value="Food & Beverages">Food & Beverages</option>
        <option value="Health & Beauty">Health & Beauty</option>
        <option value="Other">Other</option>
      </select>
      <input type="text" name="imageUrl" placeholder="Image URL (optional)">
      <button type="submit" class="btn btn-success">Add Product</button>
    </form>

    <form action="/products/admin/dashboard" method="GET" class="create-product-form" style="margin-top: 20px;">
      <h3>Filter Products</h3>
      <input type="text" name="name" placeholder="Search by name" value="<%= selectedName || '' %>">
      <select name="category">
        <option value="All">All Categories</option>
        <% if (categories && categories.length) { %>
          <% categories.forEach(cat => { %>
            <option value="<%= cat %>" <%= selectedCategory === cat ? 'selected' : '' %>><%= cat %></option>
          <% }) %>
        <% } %>
      </select>
      <div style="display: flex; gap: 10px; align-items: center;">
        <button type="submit" class="btn btn-primary">Apply Filters</button>
        <a href="/products/admin/dashboard" class="btn btn-secondary" style="text-decoration: none;">Clear</a>
      </div>
    </form>

    <table class="products-table">
      <thead>
        <tr>
          <th>Image</th>
          <th>Name</th>
          <th>Category</th>
          <th>Description</th>
          <th>Price</th>
          <th>Stock</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% products.forEach(product => { %>
          <tr>
            <td><img src="<%= product.imageUrl %>" alt="<%= product.name %>" style="width: 80px; height: 60px; object-fit: cover;"></td>
            <td><%= product.name %></td>
            <td><span style="background-color: #e3f2fd; padding: 4px 8px; border-radius: 4px; font-size: 0.9em;"><%= product.category %></span></td>
            <td><%= product.description %></td>
            <td>$<%= product.price %></td>
            <td><%= product.stock %></td>
            <td>
              <div class="admin-btn-group">
                <a href="/products/<%= product._id %>/edit" class="admin-btn admin-btn-warning">Edit</a>
                <form action="/products/<%= product._id %>?_method=DELETE" method="POST" style="display:inline;">
                  <button type="submit" class="admin-btn admin-btn-danger" onclick="return confirm('Are you sure?')">Delete</button>
                </form>
              </div>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>
</html>
